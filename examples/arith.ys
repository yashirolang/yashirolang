# 演算子の優先順位を確かめる式。
# 演算子の優先順位と括弧が正しく処理されます。
#
#   ../build/<cc> arith<ext> -o arith && ./arith; echo $?
#   → 42
#
# AST を見てみると木の形が確認できます:
#   ../build/<cc> --dump-ast arith<ext>
#
# ★ <cc> はコンパイラのコマンド名、<ext> はソースの拡張子です（`make info` で分かります）。
#
2 * 3 + (10 - 4) * 6
