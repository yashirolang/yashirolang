# OUTPUT: 3
# OUTPUT: 7
# フィールドへの代入（postfix の ND_FIELD が代入先になれる）
class P:
    x: int
    y: int

def main() -> int:
    p: P = P()
    p.x = 3
    p.y = p.x + 4
    print(p.x)
    print(p.y)
    return 0
