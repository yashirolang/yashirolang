# FLAGS: -g
# OUTPUT: 5
# EXIT: 0
# IR: !DICompileUnit
# IR: !DISubprogram(name: "add"
# IR: !DILocation(line: 13
# IR: !llvm.dbg.cu
# A-30: -g でデバッグ情報（DWARF のもとになる metadata）を出します。
#   ★ 出すのは「関数の枠」と「行の対応表」だけで、これだけで
#     ブレークポイント・バックトレース・perf の行表示が効きます。
#   ⚠️ 行番号を書いた期待値です（13 行目 = 下の `return a + b`）。
#     **この下の行を動かしたら、期待値も直してください。**
def add(a: int, b: int) -> int:
    return a + b

def main() -> int:
    x: int = add(2, 3)
    print(str(x))
    return 0
