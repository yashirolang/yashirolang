# ERROR: 既定値に変数は書けません
# 既定値に名前は書けない（A-38）
#
# ★ 既定値は**呼び出し側に置き換わる**ので、名前を許すと「定義側の名前か、
#   呼び出し側の名前か」で答えが 2 つになります。
XS: int = 3

def f(a: int = XS) -> int:
    return a

def main() -> int:
    return f()
