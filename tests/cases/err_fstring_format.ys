# ERROR: f-string の中の式を解釈できません
# ERROR: 書式を付けるなら
# f-string の中の式は **最後まで読み切れること**
#
#   注意: この検査が無いと、読み切れなかった残りが**黙って無視**されます。
#     エラーより悪い挙動なので、必ず弾きます。
#
#   ★ 0.37.0 から書式指定（{x:>8}）は書けます。':' の後ろは書式として読むので、
#     ここで断られるのは「式そのものが読み切れない」形だけです。
def main() -> int:
    print(f"[{1 2}]")
    return 0
