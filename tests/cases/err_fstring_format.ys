# ERROR: f-string の中の式を解釈できません: '1:>8'
# ERROR: = ヒント: 書式指定（f"{x:>8}" のような桁揃え）はありません。strings.lpad / strings.rpad を使ってください
# f-string の中の式は **最後まで読み切れること**
#   ⚠️ この検査が無いと {1:>8} が「1」だけ読まれて**黙って無視**される。
#     エラーより悪い挙動なので、必ず弾く。
def main() -> int:
    print(f"[{1:>8}]")
    return 0
