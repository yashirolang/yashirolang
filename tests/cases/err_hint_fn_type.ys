# ERROR: 型が一致しません
# ERROR: = ヒント: 引数の数が違います（2 個と 1 個）
# 関数型が合わないとき、**どこが違うのか**を言う
#   注意: 「暗黙の型変換がありません」では何を直せばよいか分かりません。
def g(a: int, b: int) -> int:
    return a

def main() -> int:
    f: fn(int) -> int = g
    return 0
