# ERROR: 型が一致しません
# ERROR: = ヒント: 'C' が 'I' を実装すると宣言していません（'class C(I):' と書きます）
# メソッドは持っているのに実装宣言を書き忘れたとき
interface I:
    def f(self) -> int

class C:
    x: int
    def f(self) -> int:
        return 1

def main() -> int:
    v: I = C()
    return 0
