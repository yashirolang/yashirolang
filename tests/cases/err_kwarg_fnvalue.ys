# ERROR: 関数の値 'g' は名前で引数を受け取りません
# 関数の値には名前で渡せない（A-38）
#
# ★ 関数の型（fn(int, int) -> int）には引数の名前が入っていません。
def add(a: int, b: int) -> int:
    return a + b

def main() -> int:
    g: fn(int, int) -> int = add
    return g(a = 1, b = 2)
