# ERROR: 's' は捕まえられません（'str' 型）
# ERROR: 捕まえられるのは値型だけです
# 捕まえられるのは値型だけ（A-43）
#
# ★ str や list を捕まえると「借りものを枠の上に持ち回る」話になり、
#   元を動かせるかどうかの検査が要ります。まずは黙って壊れない範囲
#   （int / float / bool / 列挙 / 範囲型）から入れました。
def main() -> int:
    s: str = "あ"
    p: fn(int) -> bool = lambda x: len(s) > x
    return 0
