# ERROR: 並べられるのは 8 個までです
# 隠し変数の数には上限があります（C 版が 8 個の配列で持つため）
#   ⚠️ 上限は 2 つの実装で同じにしてあります（src/parser.c と selfhost/parser）。
def main() -> int:
    a: int = 1
    b: int = 2
    c: int = 3
    d: int = 4
    e: int = 5
    f: int = 6
    g: int = 7
    h: int = 8
    i: int = 9
    a, b, c, d, e, f, g, h, i = i, h, g, f, e, d, c, b, a
    return 0
