# ERROR: 型 'list[int]' にメソッド 'push' はありません
# ERROR: = ヒント: list[T] で使えるのは append / pop / insert / remove / index / reverse / clear / copy / extend です（class のメソッドは自分で定義できます）
def main() -> int:
    xs: list[int] = []
    xs.push(1)
    return 0
