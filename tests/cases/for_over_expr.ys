# OUTPUT: 3
# OUTPUT: 4
def rest(xs: list[int]) -> list[int]:
    ys: list[int] = []
    for x in xs:
        if x > 2:
            ys.append(x)
    return ys

def main() -> int:
    for x in rest([1, 2, 3, 4]):
        print(x)
    return 0
