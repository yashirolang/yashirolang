# ERROR: error[E-MOVE-1]: 移動済みの値 'xs' を使っています
# ERROR: 'xs' はここで移動しました
# A-24: 所有権の検査は **既定でエラー**です（オプションは 1 つも要りません）。
#   注意: 0.17 までは警告でした。警告に落としたいときは --warn-own です
#     （warn_move_default を参照）。
def main() -> int:
    xs: list[int] = [1, 2, 3]
    ys: list[int] = xs
    print(len(ys))
    return len(xs)
