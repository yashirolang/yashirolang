# ERROR: error[E-MUT-1]: 読み取り専用の借用 'xs' を書き換えています
# A-24: 可変性の検査も既定でエラーです（--deny-mut は要りません）。
def fill(xs: mut list[int], v: int) -> None:
    xs.append(v)

def relay(xs: list[int]) -> None:
    fill(xs, 1)

def main() -> int:
    a: list[int] = []
    relay(a)
    return len(a)
