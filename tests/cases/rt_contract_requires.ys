# STDIN: 0
# EXIT: 1
# A-29: 事前条件を破ると止まります（Ada の Constraint_Error にあたります）。
#   ⚠️ 外すオプションはありません（範囲型と同じ理由）。
def divide(a: int, b: int) -> int:
    requires b != 0
    return a // b

def main() -> int:
    n: int = int(input())
    print(str(divide(10, n)))
    return 0
