# STDIN: 200
# EXIT: 1
# A-28: 定数でない値は**実行時に**確かめます（Ada の Constraint_Error にあたります）。
#   ⚠️ 外すオプションはありません。「型で 0..100 と言ったのに 101 が入る」
#     状態を作れるなら、型で言った意味がないからです。
type Percent = int range(0, 100)

def main() -> int:
    n: int = int(input())
    a: Percent = n
    print(str(a))
    return 0
