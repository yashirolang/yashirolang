# ERROR: モジュール 'lib.Box' を import していません
# ERROR: この修飾を解決できません
# A-32: 修飾は何段でも書けるようになりましたが、**段の途中はモジュール**です。
#   ★ `lib.Box.Inner` は「モジュール lib.Box の型 Inner」と読まれます。
#     クラスの中のクラス（入れ子のクラス）はありません。
#   注意: 0.22 までは「修飾は 1 段だけです」と断っていました。
import lib

def main() -> int:
    b: lib.Box.Inner = lib.Box()
    return 0
